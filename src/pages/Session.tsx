import { Box } from '@mui/system'
import React, { useEffect, useState } from 'react'

const Session = () => {
  const [session, setSession] = useState()


  useEffect(() => {
    // window.sipapu.Session.get
  })

  return <Box>
    Session
  </Box>
}

export default Session